# Copy this file to .env and fill in ONE of the following (optional).
# If neither is set, the app still works, using a rule-based fallback
# for the AI Summary and Q&A tabs instead of a real LLM.

ANTHROPIC_API_KEY=
OPENAI_API_KEY=
